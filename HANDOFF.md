# Azoth Handoff

The canonical project plan, current session state, verification ledger, and next goal live in [`PROJECT_ROADMAP.md`](PROJECT_ROADMAP.md).

Do not reconstruct current state from this pointer, `athanasor/lapis/state.json`, or historical handoff files. Start every session by reading `AGENTS.md` and `PROJECT_ROADMAP.md`, then reconcile the roadmap with live Git, Incipere, and Vigil output.
